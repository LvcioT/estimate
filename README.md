# Scrum Planning Poker (estimate)

Simple Planning Poker web app using Go, Gin, HTMX and SQLite.

Run:

```bash
go mod tidy
go run ./
```

Open http://localhost:8080

Default admin created if none exists: username=admin password=admin
# estimate
Scrum Planning Poker
